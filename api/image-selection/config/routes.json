{
  "routes": [
    {
      "method": "GET",
      "path": "/image-selections",
      "handler": "image-selection.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/image-selections/count",
      "handler": "image-selection.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/image-selections/:id",
      "handler": "image-selection.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/image-selections",
      "handler": "image-selection.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/image-selections/:id",
      "handler": "image-selection.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/image-selections/:id",
      "handler": "image-selection.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
